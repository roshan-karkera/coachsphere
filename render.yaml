services:
  - type: web
    name: coachsphere-mcp
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt && pip install "mcp[cli]>=1.0.0"
    startCommand: python mcp/server.py
    envVars:
      - key: MCP_TRANSPORT
        value: sse
